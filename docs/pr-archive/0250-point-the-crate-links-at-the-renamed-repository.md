# #250 — Point the crate links at the renamed repository

Merged 2026-09-10 into `master` from `chore/point-the-links-at-the-renamed-repo`, commit [`0ea2ff0`](https://github.com/ulo-rs/ulo/commit/0ea2ff003a04b96e8a55b6c928f724804f564c49).

`repository` and `homepage` name `ifeanyi-ugwu/ulo`, the repository this is. The eleven URLs across ten files were the last thing still spelling the framework's old name.

GitHub redirects `toni-rs` to `ulo`, so the previous URLs resolved. A published crate names the repository rather than a redirect to it.
