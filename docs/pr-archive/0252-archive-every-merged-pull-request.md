# #252 — Archive every merged pull request

Merged 2026-09-10 into `master` from `docs/archive-the-pull-requests`, commit [`7337126`](https://github.com/ulo-rs/ulo/commit/733712682f8297830a7e02ec11d8d425a63af86a).

`docs/pr-archive/` holds all 251 merged pull requests, each with its description as written, indexed newest-first in a README.

Detaching this repository from its fork network deletes its pull requests and keeps every commit. These descriptions are the part of the record GitHub alone holds; the 251 PRs carry no comments, so the descriptions are the whole of it.

Descriptions are reproduced verbatim, so the 145 written before ADR-0044 name the framework `toni`. A process record describes what was written at the time and is not corrected in place.
