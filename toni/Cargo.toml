[package]
name = "toni"
version = "0.2.0"
edition.workspace = true
keywords = ["http", "web", "framework"]
categories = ["asynchronous", "network-programming", "web-programming"]
description = "Fast and modular web framework for scalable applications"
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"

[features]
default = ["logger"]
logger = ["dep:tracing-subscriber"]
tower-compat = ["dep:tower"]

[dependencies]
serde = { version = "1.0.216", features = ["derive"] }
serde_json = "1.0.134"
rustc-hash = "2.1"
anyhow = "1.0.95"
async-trait = "0.1.86"
validator = "0.20.0"
serde_urlencoded = "0.7"
thiserror = "2.0"
parking_lot = "0.12"
event-listener = "5"
base64 = "0.22"
futures = { version = "0.3", default-features = false, features = ["std"] }
bytes = "1"
http-body = "1"
http-body-util = "0.1"
toni-macros = { path = "../toni-macros" }
uuid = { version = "1.0", features = ["v4"] }
http = { version = "1" }
tower = { version = "0.5", default-features = false, features = ["util"], optional = true }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"], optional = true }
multer = "3"

[dev-dependencies]
tokio = { version = "1.42.0", features = ["full"] }
# Drives the `StartupCheck` tests: it takes no runtime, so proving that needs an executor rather
# than a runtime.
futures-executor = "0.3"
reqwest = { version = "0.12.11", features = ["json"] }
